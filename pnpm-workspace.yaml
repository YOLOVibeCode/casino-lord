packages:
  - "packages/*"
  - "apps/*"

# pnpm 10 blocks lifecycle scripts by default; esbuild needs its postinstall
# to fetch the platform binary that vitest depends on.
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
